wget https://xorg.freedesktop.org/releases/individual/lib/libxshmfence-1.3.3.tar.xz
tar -xf libxshmfence-1.3.3.tar.xz
cd libxshmfence-1.3.3

./configure \
    --host=aarch64-linux-gnu \
    --build=x86_64-linux-gnu \
    --prefix=/data/data/com.winlator/files/imagefs/usr \
    --disable-static \
    --with-shared-memory-dir=/tmp

make -j$(nproc)
make install INSTALL_PROGRAM="/usr/bin/install -c"
cd ..