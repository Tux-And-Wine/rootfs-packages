# 下载并解压最新版 SPIRV-Tools
wget https://github.com/KhronosGroup/SPIRV-Tools/archive/refs/tags/v2026.1.tar.gz -O spirv-tools-2026.1.tar.gz
tar -xzf spirv-tools-2026.1.tar.gz
cd SPIRV-Tools-2026.1

# 创建交叉编译工具链文件
cat > toolchain-aarch64.cmake << 'EOF'
set(CMAKE_SYSTEM_NAME Linux)
set(CMAKE_SYSTEM_PROCESSOR aarch64)
set(CMAKE_SYSROOT /data/data/com.winlator/files/imagefs)
set(CMAKE_C_COMPILER aarch64-linux-gnu-gcc)
set(CMAKE_CXX_COMPILER aarch64-linux-gnu-g++)
set(CMAKE_FIND_ROOT_PATH /data/data/com.winlator/files/imagefs/usr)
set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
EOF

# 配置 (Termux 风格)
cmake -B build \
    -DCMAKE_TOOLCHAIN_FILE=toolchain-aarch64.cmake \
    -DCMAKE_INSTALL_PREFIX=/data/data/com.winlator/files/imagefs/usr \
    -DSPIRV-Headers_SOURCE_DIR=/data/data/com.winlator/files/imagefs/usr \
    -DSPIRV_WERROR=OFF \
    -DSPIRV_SKIP_TESTS=ON \
    -DCMAKE_BUILD_TYPE=Release

# 编译安装
cmake --build build --parallel $(nproc)
cmake --install build
cd ..